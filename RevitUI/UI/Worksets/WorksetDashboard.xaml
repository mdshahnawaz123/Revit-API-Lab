<Window x:Class="RevitUI.UI.Worksets.WorksetDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Workset Pro" Height="600" Width="700"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Background="#F5F6F7" WindowStyle="None" AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="20,15">
                <TextBlock Text="Workset Pro" HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" Foreground="#222"/>
                <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" BorderThickness="0" Foreground="#888" FontSize="18" Cursor="Hand"/>
            </Grid>

            <Grid Grid.Row="1" Margin="20,0,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Controls -->
                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="Select Category" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CbCategory" ItemsSource="{Binding Categories}" SelectionChanged="CbCategory_SelectionChanged"/>

                    <TextBlock Text="Select Element Type" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CbElementType" ItemsSource="{Binding ElementTypes}" SelectionChanged="CbElementType_SelectionChanged"/>

                    <CheckBox x:Name="ChkAssignAll" Content="Assign to ALL types in category" Margin="0,0,0,20" IsChecked="{Binding AssignToAll}"/>

                    <TextBlock Text="Select Workset" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="CbWorkset" ItemsSource="{Binding Worksets}" SelectedItem="{Binding SelectedWorkset}"/>

                    <Button Content="Assign Workset" Style="{StaticResource ModernButton}" Height="40" Margin="0,0,0,10" Click="BtnAssign_Click"/>
                    <Button Content="Export to CSV" Style="{StaticResource ModernButton}" Height="40" Click="BtnExport_Click"/>
                </StackPanel>

                <!-- Right Panel: DataGrid -->
                <Border Grid.Column="1" BorderBrush="#EEE" BorderThickness="1" CornerRadius="4">
                    <DataGrid x:Name="DgElements" AutoGenerateColumns="False" CanUserAddRows="False" 
                              Background="White" BorderThickness="0" GridLinesVisibility="Horizontal" 
                              HorizontalGridLinesBrush="#F0F0F0" HeadersVisibility="Column"
                              RowHeight="30" FontSize="12">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Foreground" Value="#666"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#EEE"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Element ID" Binding="{Binding Id}" Width="100"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="*"/>
                            <DataGridTextColumn Header="Workset" Binding="{Binding WorksetName}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>

