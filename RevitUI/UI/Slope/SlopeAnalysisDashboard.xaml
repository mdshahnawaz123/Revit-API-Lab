<Window x:Class="RevitUI.UI.Slope.SlopeAnalysisDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Slope Analysis" Height="650" Width="420"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Professional Light Colors -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="HeaderColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#111827"/>
        <SolidColorBrush x:Key="MutedText" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedText}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,18"/>
            
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColorBox" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundColor}" CornerRadius="16" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="65"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource HeaderColor}" CornerRadius="16,16,0,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
                <Grid Margin="25,0">
                    <TextBlock Text="SLOPE ANALYSIS" Foreground="{StaticResource TextColor}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#9CA3AF" FontSize="20" Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30">
                    <TextBlock Text="ANALYSIS TARGETS" Style="{StaticResource SectionHeader}"/>
                    <CheckBox x:Name="ChkFloors" Content="Include Floors" IsChecked="True"/>
                    <CheckBox x:Name="ChkRoofs" Content="Include Roofs" IsChecked="True"/>
                    <CheckBox x:Name="ChkTopo" Content="Include Topography" IsChecked="True"/>

                    <Border Height="1" Background="{StaticResource BorderColor}" Margin="0,15,0,30"/>

                    <TextBlock Text="VISUALIZATION" Style="{StaticResource SectionHeader}"/>
                    <RadioButton x:Name="RbNone" Content="Calculate Only (No Coloring)"/>
                    <RadioButton x:Name="RbHeatMap" Content="Apply Slope Heatmap" IsChecked="True"/>

                    <StackPanel Orientation="Horizontal" Margin="0,15,0,30">
                        <TextBlock Text="Angle Unit:" VerticalAlignment="Center" Margin="0,0,20,0" FontWeight="Medium"/>
                        <ComboBox x:Name="ComboUnit" Width="160" Height="40">
                            <ComboBoxItem Content="Degrees (°)" IsSelected="True"/>
                            <ComboBoxItem Content="Percentage (%)"/>
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Text="COLOR THRESHOLDS" Style="{StaticResource SectionHeader}"/>
                    
                    <!-- Range 1 -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Flat / Low Slope" Foreground="{StaticResource MutedText}" FontSize="10" FontWeight="Bold" Margin="2,0,0,6"/>
                            <TextBox x:Name="TxtT1" Text="0" Height="40" Margin="0,0,20,0"/>
                        </StackPanel>
                        <Border x:Name="PickC1" Background="#10B981" Style="{StaticResource ColorBox}" Grid.Column="1" VerticalAlignment="Bottom" MouseLeftButtonDown="ColorPicker_Click"/>
                    </Grid>

                    <!-- Range 2 -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Medium Slope" Foreground="{StaticResource MutedText}" FontSize="10" FontWeight="Bold" Margin="2,0,0,6"/>
                            <TextBox x:Name="TxtT2" Text="5" Height="40" Margin="0,0,20,0"/>
                        </StackPanel>
                        <Border x:Name="PickC2" Background="#F59E0B" Style="{StaticResource ColorBox}" Grid.Column="1" VerticalAlignment="Bottom" MouseLeftButtonDown="ColorPicker_Click"/>
                    </Grid>

                    <!-- Range 3 -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Steep Slope" Foreground="{StaticResource MutedText}" FontSize="10" FontWeight="Bold" Margin="2,0,0,6"/>
                            <TextBox x:Name="TxtT3" Text="15" Height="40" Margin="0,0,20,0"/>
                        </StackPanel>
                        <Border x:Name="PickC3" Background="#EF4444" Style="{StaticResource ColorBox}" Grid.Column="1" VerticalAlignment="Bottom" MouseLeftButtonDown="ColorPicker_Click"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="White" CornerRadius="0,0,16,16" Padding="30,20" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
                <Button Content="START ANALYSIS" Style="{StaticResource ModernButton}" Height="50" Click="BtnRun_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
