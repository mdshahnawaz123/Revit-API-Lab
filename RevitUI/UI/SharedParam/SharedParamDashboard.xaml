<Window x:Class="RevitUI.UI.SharedParam.SharedParamDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Shared Parameter Manager" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0B192C" AllowsTransparency="True" WindowStyle="None">

    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="Border" Width="18" Height="18" CornerRadius="4" 
                                        Background="#333" BorderBrush="#555" BorderThickness="1">
                                    <Path x:Name="CheckMark" Data="M 0,5 L 3,8 L 8,0" Stroke="#007ACC" 
                                          StrokeThickness="2" Margin="4,4,0,0" Visibility="Collapsed"/>
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0088EE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        Background="{TemplateBinding Background}" 
                                        Foreground="{TemplateBinding Foreground}"
                                        Grid.Column="2" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                        Focusable="false" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1A1A1A" BorderBrush="#333" BorderThickness="1" CornerRadius="0,0,4,4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" BorderBrush="#333" BorderThickness="1" Background="#121212">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="25,20,25,10">
                <StackPanel>
                    <TextBlock Text="SHARED PARAMETER MANAGER" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Bulk-apply shared parameters across categories" Foreground="#007ACC" FontSize="13" Margin="0,4,10,0"/>
                        <Button Content="?" Click="BtnHelp_Click" Width="20" Height="20" Background="#333" Foreground="White" 
                                BorderThickness="0" FontSize="12" FontWeight="Bold" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
                <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" Foreground="#666" BorderThickness="0" FontSize="20" Cursor="Hand"/>
            </Grid>

            <!-- File Loader -->
            <Grid Grid.Row="1" Margin="25,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="CmbFileHistory" Grid.Column="0" Height="38" IsEditable="True" 
                         VerticalContentAlignment="Center" Padding="10,0"
                         Background="#1A1A1A" Foreground="White" BorderBrush="#333"
                         Text="Select a shared parameter file (.txt)...">
                </ComboBox>
                <Button Grid.Column="1" Content="BROWSE" Style="{StaticResource SecondaryButton}" Width="90" Height="36" Margin="10,0,0,0" Click="BtnBrowse_Click" Cursor="Hand"/>
                <Button Grid.Column="2" Content="LOAD" Style="{StaticResource ActionButton}" Width="70" Height="36" Margin="10,0,0,0" Click="BtnLoad_Click" Cursor="Hand"/>
            </Grid>

            <!-- Two-Panel Layout -->
            <Grid Grid.Row="2" Margin="25,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Parameters -->
                <Border Grid.Column="0" Background="#1A1A1A" CornerRadius="10" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock Text="PARAMETERS" Foreground="#007ACC" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Export CSV" Style="{StaticResource SecondaryButton}" Padding="8,2" FontSize="10" Margin="0,0,5,0" Click="BtnExportCsv_Click"/>
                                <Button Content="Import CSV" Style="{StaticResource SecondaryButton}" Padding="8,2" FontSize="10" Margin="0,0,5,0" Click="BtnImportCsv_Click"/>
                                <Button Content="+ Create New" Style="{StaticResource SecondaryButton}" Padding="8,2" FontSize="10" Click="BtnShowCreateForm_Click"/>
                            </StackPanel>
                        </Grid>

                        <!-- New Parameter Form (Initially Hidden) -->
                        <Border x:Name="CreateParamForm" Grid.Row="1" Background="#222" CornerRadius="4" Padding="10" Margin="0,0,0,12" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="Parameter Type" Foreground="#888" FontSize="10" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <RadioButton x:Name="RbNewProjectParam" Content="Project Parameter" Foreground="White" IsChecked="True" Margin="0,0,15,0" FontSize="10"/>
                                    <RadioButton x:Name="RbNewSharedParam" Content="Shared Parameter" Foreground="White" FontSize="10"/>
                                </StackPanel>

                                <TextBlock Text="Name (one per line or comma separated)" Foreground="#888" FontSize="10" Margin="0,0,0,2"/>
                                <TextBox x:Name="TxtNewParamName" Background="#121212" Foreground="White" BorderBrush="#444" 
                                         Padding="6,4" Margin="0,0,0,8" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                                
                                <TextBlock Text="Data Type" Foreground="#888" FontSize="10" Margin="0,0,0,2"/>
                                <ComboBox x:Name="CmbNewParamType" Background="#121212" Foreground="White" BorderBrush="#444" Margin="0,0,0,10"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,4,0" Click="BtnCancelCreate_Click"/>
                                    <Button Grid.Column="1" Content="Create" Style="{StaticResource ActionButton}" Height="28" Click="BtnCreateParam_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="2" Background="#121212" CornerRadius="4" BorderBrush="#333" BorderThickness="1" Margin="0,0,0,12">
                            <TextBox x:Name="TxtSearchParam" Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,6" TextChanged="TxtSearchParam_TextChanged"/>
                        </Border>
                        
                        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="ParamList"/>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Right: Categories -->
                <Border Grid.Column="2" Background="#1A1A1A" CornerRadius="10" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="TARGET CATEGORIES" Foreground="#007ACC" FontSize="12" FontWeight="Bold" Margin="0,0,0,12"/>

                        <Border Grid.Row="1" Background="#121212" CornerRadius="4" BorderBrush="#333" BorderThickness="1" Margin="0,0,0,8">
                            <TextBox x:Name="TxtSearchCat" Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,6" 
                                     TextChanged="TxtSearchCat_TextChanged" Tag="Search categories..."/>
                        </Border>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="Select All" Click="BtnSelectAll_Click" Style="{StaticResource SecondaryButton}" Padding="8,2" FontSize="10" Margin="0,0,8,0"/>
                            <Button Content="Clear All" Click="BtnClearAll_Click" Style="{StaticResource SecondaryButton}" Padding="8,2" FontSize="10"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="CategoryList"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- Options Bar -->
            <Grid Grid.Row="3" Margin="25,0,25,15">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Binding:" Foreground="#666" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <RadioButton x:Name="RbInstance" Content="Instance" Foreground="White" IsChecked="True" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <RadioButton x:Name="RbType" Content="Type" Foreground="White" VerticalAlignment="Center" Margin="0,0,40,0"/>
                    <TextBlock Text="Group:" Foreground="#666" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <ComboBox x:Name="CmbGroup" Width="200" Height="32" Background="#1A1A1A" Foreground="White" BorderBrush="#333"/>
                </StackPanel>
            </Grid>

            <!-- Status & Action -->
            <Grid Grid.Row="4" Margin="25,0,25,25">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="Ready" Foreground="#666" FontSize="12" Margin="0,0,20,0"/>
                    <Border Background="#222" CornerRadius="4" Padding="8,2">
                        <TextBlock Text="License: ACTIVE" Foreground="#007ACC" FontSize="10" FontWeight="Bold"/>
                    </Border>
                </StackPanel>
                <Button Content="APPLY TO PROJECT" Style="{StaticResource ActionButton}" Width="220" Height="48" HorizontalAlignment="Right" Click="BtnApply_Click" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Border>
</Window>
